# In-Memory Key-Value Хранилище на Go

Мини-версия Redis, написанная на чистом Go.  
Подходит для обучения, прототипирования и использования в качестве лёгкого кеша, например, в ML-пайплайнах.

## ✨ Возможности

- `SET`, `GET`, `DEL` — базовые операции с ключами
- `SETEX key seconds value` — установка с TTL (временем жизни)
- `PING` → `PONG` — проверка работоспособности
- Простой текстовый протокол по TCP (в стиле RESP)
- Потокобезопасный доступ через `sync.RWMutex`
- Корректное завершение при `SIGTERM` (graceful shutdown)
- Готов к Docker: запускается в контейнере
- Автоматизация через Makefile: `make test`, `make run`, `make docker-run`

## 🚀 Быстрый старт

### Запуск локально
```bash
make build
make run
